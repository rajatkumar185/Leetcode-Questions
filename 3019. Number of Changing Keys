class Solution {
public:
    int countKeyChanges(string s) {
        int count=0;
        for(int i=0;i<s.size()-1;i++){
            int a=tolower(s[i]);
            int b=tolower(s[i+1]);
            if(a!=b){
                count++;
            }
        }
        return count;
    }
};
