class Solution {
    public int maxProductDifference(int[] nums) {
        Arrays.sort(nums);
        int firstPair=nums[nums.length-1]*nums[nums.length-2];
        int secondPair=nums[1]*nums[0];
        return firstPair-secondPair;
        
    }
}
